#include "Continent.h"
#include "Node.h"
#include "Graph.h"
#include "Player.h"
#include "Army.h"

#include<iostream>
#include<string>
#include<vector>

using namespace std;

int main()
{
	cout << "TESTING!" << endl;
	


	//vector<Node*> countryVector;
	//countryVector.push_back(new Node("Canada", 1, null, 0));
	//string name, int &continent, vector<Node*> adjacent, int code
	
	/*Continent ameristralia = Continent("AMERISTRALIA", 1);
	Node canada = Node("Canada", &ameristralia);
	Node america = Node("America", &ameristralia);
	Node australia = Node("Australia", &ameristralia);

	Continent europe = Continent("Europe", 2);
	Node britain = Node("Britain", &europe);
	Node spain = Node("Spain", &europe);

	canada.adjacent.push_back(&america);
	canada.adjacent.push_back(&britain);

	america.adjacent.push_back(&canada);
	america.adjacent.push_back(&britain);
	america.adjacent.push_back(&australia);

	britain.adjacent.push_back(&canada);
	britain.adjacent.push_back(&america);
	britain.adjacent.push_back(&spain);

	spain.adjacent.push_back(&britain);

	australia.adjacent.push_back(&america);

	Graph graph;
	graph.countries.push_back(&canada);
	graph.countries.push_back(&america);
	graph.countries.push_back(&australia);
	graph.countries.push_back(&britain);
	graph.countries.push_back(&spain);*/

	return 0;
}